// Line art demo
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255);
}

function draw(){
}

function drop_ball(){
  if (keyIsPressed){
    console.log("not here")
    if (key === 'b'){
      fill(255,0,255)
      ellipse(mouseX, mouseY, 35, [h])
    }
  }
}